import Logo from "../logo/logo";
import * as C from "./styles";

export const Footer = () => {
  return (
    <C.containerFooter>
      <Logo></Logo>
    </C.containerFooter>
  );
};

export default Footer;
